# railway.toml — monorepo deployment config
[build]
builder = "NIXPACKS"
buildCommand = "npm run install:all && npm run build && npm run db:generate"

[deploy]
startCommand = "npm run db:migrate && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
